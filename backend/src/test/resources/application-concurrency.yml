spring:
  datasource:
    hikari:
      maximum-pool-size: 110
      minimum-idle: 0
      connection-timeout: 30000

  jpa:
    show-sql: false

logging:
  level:
    org.hibernate.SQL: warn
    org.hibernate.orm.jdbc.bind: warn
