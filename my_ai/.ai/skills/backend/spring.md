# Spring Skill

## 기본 원칙

- Controller는 HTTP 요청/응답과 입력 검증, 인증 정보 전달 등 웹 계층 책임에 집중한다.
- 비즈니스 로직은 Service에 둔다.
- 데이터 접근은 Repository에 둔다.
- 웹 기술을 Service에 직접 결합하지 않는다.
- Entity를 외부 API 응답으로 직접 반환하지 않는다.
- 새로운 라이브러리나 아키텍처 패턴을 도입하기 전에 현재 Spring 구조로 해결 가능한지 검토한다.
- 기존 프로젝트의 패키지 구조와 컨벤션을 먼저 확인한다.
